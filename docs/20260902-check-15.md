# .gemap 与 sidecar 职责验收报告（第 15 次）

- **日期**：2026-09-02
- **目标**：明确当前发布输出中 `.gemap`、`world.json`、chunk sidecar 的权威职责，并提供机器校验。

## 正式职责

```text
world.gemap
  = identity anchor：稳定 gemap v3 容器、world revision、sidecar 契约引用

sidecar.json + world.json + scenes/**
  = terrain-authoritative sidecar：heightfield、surface、LOD、scene/entity/landmark 索引
```

新增 `SidecarContract`、根目录 `sidecar.json`、gemap metadata 职责字段，以及：

```text
glyphweave validate-world WORLD_DIR
```

## 验收清单

- [x] sidecar 有 format/version/高度精度/权威职责声明。
- [x] `.gemap` metadata 记录 revision、sidecar 和 identity-anchor 角色。
- [x] validate-world 校验 world/sidecar/gemap 的 revision、scene list 和 payload 存在性。
- [x] 输出不再隐含地把 `.gemap` 误称为完整地形。

## 当前限制

- [ ] `.gemap` 仍不是 terrain payload 的完整归档。
- [ ] 还没有 pack/unpack 将 sidecar 封装为完整 release container。

## 判定

**通过职责明确与一致性验证；未通过单文件完整发布归档。**
