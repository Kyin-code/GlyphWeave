# 真实 Manifest 生成基线验收报告（第 17 次）

- **日期**：2026-09-02
- **目标**：使用真实世界示例而非只用单元测试，建立可复现的生成统计与 chunk hash 基线。

## 已建立基线

目录：`docs/world-baselines/`

- `west-lake.baseline.json`
- `shenzhen-bay.baseline.json`
- `inner-mongolia-steppe.baseline.json`
- `shenzhen-bay-gis.baseline.json`

每份基线记录：输入 Manifest SHA-256、world revision、sidecar contract、场景尺寸、chunk/landmark/entity 数量、entity kind 分布、chunk hash 和 quality-report warnings。

新增：

```text
glyphweave check-baseline BASELINE.json WORLD_DIR
```

该命令校验 scene、entity/landmark 数量、kind 分布和 chunk hash。

## 实测

四个真实 Manifest 都已运行：

```text
generate-world
validate-world
quality-report
check-baseline
```

且均通过 baseline 比对。

## 参数发现

基线明确暴露当前参数问题：

- 深圳湾 profile 的 urban/nature 面积份额偏离目标；
- 内蒙古草原 profile 的 rural 份额为 0，几乎全为自然对象；
- quality-report 均提示尚未生成 `scale-audit.json`。

这些不是基线失败，而是下一轮参数校准的真实证据。

## 判定

**通过。** 已有 4 个真实 Manifest 的可复现回归基线，并明确记录当前参数偏差。
